// Shared combat configuration
export const COMBAT_CONFIG = {
    // Collision detection
    treeCollisionRadius: 1.0,
    totemCollisionRadius: 0.5,
    playerRadius: 0.8,
    
    // Enemy spawn
    spawnRadius: 2.5, // Minimum distance from trees
    maxSpawnAttempts: 10
};
