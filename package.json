{
  "name": "cats-of-the-rainforest",
  "version": "1.0.0",
  "type": "module",
  "description": "A strategy and tower-defense game where a cat tribe protects a rainforest clearing",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "itch:zip": "npm run build -- --base=./ && cd dist && zip -r ../cats-of-the-rainforest-itch.zip ."
  },
  "dependencies": {
    "three": "^0.169.0"
  },
  "devDependencies": {
    "vite": "^5.4.0"
  }
}
